>>如果用gap，存在的弊端：只需经过数次LB迭代，就可利用CPLEX求解集合覆盖得到最/较优解，
而此时LB并不太高，导致gap较大，所以余下的时间都在不断提高下界来缩小gap，但实际已获得较优的上界解
这也是上下界这种策略的劣势，很多时间用在了证明解的最优性
>>替代方案
1. 固定迭代次数
2. 设定上界变化最大迭代次数k，若在k次内，上界的变化幅度小于设定值e，则stop
3. 设定下界变化最大迭代次数k，若在k次内，下界的变化幅度小于设定值e，则求上界（或是每隔一定迭代次数求上界），stop


最好是自适应
